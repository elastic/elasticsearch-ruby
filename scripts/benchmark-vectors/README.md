= Vector 64 benchmark

This benchmark script tests `client.pack_dense_vector` in the client. It is to be run with Elasticsearch 9.3.0-SNAPSHOT.
It downloads the data and produces a json file: `results.json`.
